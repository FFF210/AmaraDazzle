<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.adminPayment">

	<!-- 결제 성공 후 배너 신청 -->
	<insert id="insertAdminPay" parameterType="adminPayment">
		insert into
		admin_payment (payment_id, payment_type, payment_key, brand_id, amount,
		plan_id, banner_id, pay_date, lastautopay_date)
		values(0, #{paymentId},#{paymentType}, #{paymentKey}, #{brandId}, #{amount},
		#{planId}, #{bannerId}, #{payDate}, #{lastAutopayDate} )
	</insert>



</mapper>