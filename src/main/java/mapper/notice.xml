<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.notice">

	<!-- 공지(seller) 저장 -->
	<insert id="insertSellerNotice" parameterType="notice" >
		insert into notice	values (0, #{typeId}, #{title}, #{content},#{image1FileId},#{image2FileId}, #{image3FileId}, #{writer}, #{targetTypeId}, 1, now(), null)
	</insert>
</mapper>