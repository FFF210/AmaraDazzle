<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.payment">

<!-- 결제 정보 저장 -->
<insert id="insertPayment" parameterType="dto.Payment">
    INSERT INTO payment (
        order_id, 
        provider, 
        method, 
        amount, 
        status, 
        payment_key
    ) VALUES (
        #{orderId}, 
        #{provider}, 
        #{method}, 
        #{amount}, 
        #{status}, 
        #{paymentKey}
    )
</insert>

</mapper>