<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="mapper.banner">
	<insert id = "insertBanner" parameterType="banner">
		insert into banner (banner_id, brand_id, manager_name, manager_tel, start_date, end_date, banner_name, status) values(#{banner_id},#{brand_id}, #{manager_name}, #{start_date}, #{end_date}, #{banner_name}, #{status}, #{}, #{} )
	</insert>

</mapper>